"""Git import + sync + backup services (grouped sub-package).

Modules: backup, book_serializer, credentials, import_adopter,
import_inspector, sync_commit, sync_diff, sync_lock, sync_mapping,
sync_markdown_utils, sync_unified.
"""
