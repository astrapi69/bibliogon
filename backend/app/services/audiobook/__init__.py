"""Audiobook / TTS services (grouped sub-package).

Modules: credentials (ElevenLabs key storage + verification),
skip_types (per-book chapter-type skip resolution), synthesis
(dry-run sample generation), google_tts_setup (Google Cloud TTS
credential setup).
"""
