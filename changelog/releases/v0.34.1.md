# Bibliogon v0.34.1 — 2026-05-18

The "routine dependency refresh" point release. Pure lock-file refresh; no pyproject.toml / package.json edits; no API changes; no user-visible behavior changes. Closes the dep-refresh track deliberately deferred from the v0.34.0 release cycle.

Verification: backend pytest 1891/1891 + Vitest 1405/1405 + launcher PyInstaller build successful + `make verify-plugin-locks` confirms zero plugin drift.

## Before you install

Bibliogon runs in Docker. You need Docker Desktop installed and running before starting the launcher.

- [Docker installation guide (English)](https://github.com/astrapi69/bibliogon/blob/main/docs/help/en/install/docker-desktop.md) — includes a "Is Docker safe to install?" section
- [Docker-Installationsanleitung (Deutsch)](https://github.com/astrapi69/bibliogon/blob/main/docs/help/de/install/docker-desktop.md) — mit Abschnitt "Ist Docker sicher zu installieren?"

The launcher detects Docker, downloads Bibliogon automatically, and opens it in your browser.

## Download

| Platform | File |
|----------|------|
| Windows | `bibliogon-launcher.exe` |
| macOS (Apple silicon) | `bibliogon-launcher-macos.zip` |
| Linux | `bibliogon-launcher-linux` (ELF binary) |

Each platform also ships a `*.sha256` checksum next to the binary.

## Verifying downloads

```bash
# macOS / Linux
shasum -a 256 bibliogon-launcher-<platform>
cat bibliogon-launcher-<platform>.sha256
```

```powershell
# Windows
Get-FileHash -Algorithm SHA256 .\bibliogon-launcher.exe
Get-Content .\bibliogon-launcher.exe.sha256
```

The hashes must match.

If your operating system warns about an unsigned binary, see the [Bibliogon installation overview](https://github.com/astrapi69/bibliogon/blob/main/docs/help/en/installation.md).

## Action required

None. Patch bump; no schema migrations, no API surface changes, no behavior changes.

## What's new

Routine dependency refresh applied via the conservative `poetry update <allowlist>` discipline (per the "never blind bulk apply" lessons-learned rule). All bumps fit within existing pyproject pin constraints — no caret loosening in this session.

**Backend (6 packages):**

| Package | From | To | Note |
|---------|------|----|------|
| `pip` | 26.0.1 | 26.1.1 | Tooling patch |
| `numpy` | 2.4.4 | 2.4.5 | Patch |
| `pandas` | 3.0.2 | 3.0.3 | Patch |
| `requests` | 2.34.0 | 2.34.2 | Patch |
| `decorator` | 5.2.1 | 5.3.0 | Minor |
| `librt` | 0.9.0 | 0.11.0 | mypy transitive |

**Launcher (2 packages):**

| Package | From | To |
|---------|------|----|
| `packaging` | 26.1 | 26.2 |
| `pyinstaller-hooks-contrib` | 2026.4 | 2026.5 |

**Frontend (1 package):**

| Package | From | To | Note |
|---------|------|----|------|
| `dompurify` | 3.4.3 | 3.4.4 | Patch; `package.json` unchanged (existing `^3.4.0` caret already permitted) |

**Per-plugin lock files:** unchanged. None of the plugin `pyproject.toml` files declare any of the bumped packages as direct dependencies, so `make verify-plugin-locks` confirmed zero drift.

## Deferred to future sessions

These were *intentionally* skipped in this session and have their own tracks:

- **Backend majors:** `elevenlabs` 0.2 → 2.47 (own track), `mypy` 1.20 → 2.1 (own track), `weasyprint` 66 → 68 (just landed 66 in v0.34.0; defer to next cycle for stability soak).
- **Backend pin-loosenings:** `uvicorn` 0.46 → 0.47 and `python-multipart` 0.0.27 → 0.0.29 are blocked by the `^` caret constraints in `backend/pyproject.toml`. Loosening a pin is a separate decision class from a lock-file-only refresh and gets its own commit when a real reason surfaces.
- **Frontend TipTap 2 → 3** across ~25 packages (DEP-02 blocked-track; multi-session migration).
- **Frontend other majors:** `@types/node` 24 → 25 (cascade-into-tsconfig rule, defer), `@vitejs/plugin-react` 5 → 6 (major), `tiptap-footnotes` 2 → 3 (paired with TipTap migration), `@pentestpad/tiptap-extension-figure` 1.0 → 1.1 (peer-dep on TipTap 3).

## Lessons-learned applied (no new rules)

- **"Pre-flight a single instance before bulk-applying."** Backend was bumped first, verified with `make test-backend` (1891/1891 green), then launcher, then frontend. No bulk `poetry update` invocation.
- **"Transitive deps can surface high-risk packages from low-risk direct bumps."** The allowlist (`poetry update click decorator librt numpy pandas pip python-multipart requests uvicorn`) constrained which packages could move; transitives moved only when their own version constraint demanded it. No starlette-class surprise.
- **"Two installation paths diverge: `make test` vs per-plugin CI."** `make verify-plugin-locks` ran post-bump to confirm zero plugin drift. No plugin `pyproject.toml` files were modified, so per-plugin locks did not need refresh.
