# Bibliogon v0.58.0 — 2026-06-26

The **desktop-launcher library-first + KDP-publishing-depth + SEO** release.
The hand-written desktop launcher is replaced by the reusable
`docker-app-launcher` PyPI package, the KDP Publishing Wizard gains Format
and upload-guide steps with a WeasyPrint-rendered print PDF at the chosen
trim size + margins, and the PWA + export engine gain SEO meta tags +
JSON-LD. No schema migrations; existing data + `.bgb`/`.bgp` are unaffected.

## Before you install

Bibliogon runs in Docker. You need Docker Desktop installed and running before starting the launcher.

- [Docker installation guide (English)](https://github.com/astrapi69/bibliogon/blob/main/docs/help/en/install/docker-desktop.md) — includes a "Is Docker safe to install?" section
- [Docker-Installationsanleitung (Deutsch)](https://github.com/astrapi69/bibliogon/blob/main/docs/help/de/install/docker-desktop.md) — mit Abschnitt "Ist Docker sicher zu installieren?"

The launcher manages Docker for a local copy of the Bibliogon repository: it builds the Docker images from your copy and opens the app in your browser. You need the Bibliogon source on disk first (a `git clone`, or the source ZIP unpacked — default `~/bibliogon`); the launcher does not download Bibliogon itself. The first build takes 5-10 minutes (~2 GB disk space).

You do not need the desktop app to try Bibliogon: the offline web app at [astrapi69.github.io/bibliogon/](https://astrapi69.github.io/bibliogon/) runs entirely in your browser, with no install and no backend.

## Download

| Platform | File |
|----------|------|
| Windows | `bibliogon-launcher.exe` |
| macOS (Apple silicon) | `bibliogon-launcher-macos.zip` |
| Linux | `bibliogon-launcher-linux` (ELF binary) |

Each platform also ships a `*.sha256` checksum next to the binary.

## Verifying downloads

```bash
# macOS / Linux
shasum -a 256 bibliogon-launcher-<platform>
cat bibliogon-launcher-<platform>.sha256
```

## Action required

**None.** v0.58.0 ships no schema migration. Existing data, backups (`.bgb`) and projects (`.bgp`) are unaffected.

## What's new

### Added
- **Desktop launcher → library-first (`docker-app-launcher`).** The hand-written launcher is replaced by the reusable `docker-app-launcher` PyPI package (0.12.1); Bibliogon ships only `launcher/launcher.json` + a thin wrapper. A persistent single window, Docker-first check + guided start, install progress, port-conflict handling, a management dialog (reachable Stop/Uninstall when running), single-instance lock, rotating logging, platform-specific Docker diagnostics, a copy-log button, an always-available cleanup action, and an always-visible button-state relayout with tooltips. (#519, #521, #588–#618)
- **KDP Publishing Wizard — Format + upload-guide steps.** Choose eBook / Taschenbuch / Hardcover + a KDP trim size + margin preset (#580) and follow an upload-guide step (#581); the package PDF renders via WeasyPrint at the chosen trim + margins with crop/bleed marks (eBook → EPUB only). The package `metadata.json` carries isbn / format / trim_size / page_count / generated_by / generated_at. (#583, #606, #612, #614)
- **SEO — meta tags + JSON-LD** for the PWA and the client-side export engine across every export format (#605/#607); Git-Sync chapter `.md` files gain YAML front-matter (#614); a 1200×630 social `og-image` (#620).
- **First-install data-migration dialog** — offers to import an online `.bgb` backup on first run (PWA → desktop). (#591/#593)
- **Authoring depth** — Scrivener `.scriv` project import (#554), manual chapter collections with optional per-collection colour (#553/#572), per-chapter synopsis + project notes (#551), and per-chapter Inspector notes (#567).
- **Automated feature-screenshot catalog** — 36 on-demand capture specs (`make capture-screenshots`). (#556)

### Changed
- **God-file WARN-tier top-5 split (#207)** — `BookEditor`, `BookMetadataEditor`, `CollageCanvas`, `ArticleList`, and `ChapterOutliner` decomposed under 500 lines. (#578, #586–#590)
- **Offline grammar + translation** are gated as disabled-with-reason in Dexie mode (no browser path) instead of failing silently. (#34)
- **Launcher help-docs** corrected to the real build-from-local-repo flow (no release-ZIP download). (#603)

### Fixed
- **Picture-book client-side PDF** no longer prints raw TipTap JSON on content pages. (#616)
- **`og-image`** uses real German umlauts (`Bücher`, `offline-fähig`) and no longer clips its subtitle. (#620)
- **Chapter Outliner** loads chapter labels offline. (#548)

### Security
- Dependency bumps from `pip-audit` — `starlette`, `python-multipart`, `msgpack`. (#549)
