# Archived ROADMAP / backlog items — 2026-06

Continuous-archival monthly bucket (newest day first). See
`.claude/rules/ai-workflow.md` "Continuous archival rule".

## Archived 2026-06-01 — STORY-BIBLE-RELATIONSHIP-GRAPH-01 (P2)

The standout visual feature: an interactive React Flow graph of the
book's Story Bible — the third visual planning view alongside Arc View
(timeline) and Storyboard (pacing). Shipped in 6 commits.

- **STORY-BIBLE-RELATIONSHIP-GRAPH-01** — new dep `@xyflow/react`
  (React Flow v12, pinned 12.11.0) + `html-to-image` (1.11.13), both
  0-vuln. `?view=relationships` in BookEditor (story-bible-gated
  sidebar toggle). One node per StoryEntity (shape + colour + icon by
  type via EntityNode + storyBibleIcons; circular dependency-free
  auto-layout), one edge per relationship (coloured/labelled by type
  via relationshipColors, arrow marker). Interactive: drag-connect
  opens a type+note dialog that PATCHes the source entity's
  relationships; edge-click confirm-deletes; node-click detail panel
  (name/type/relationship-count) with open-in-editor + show-appearances;
  double-click opens the entity editor. Node positions persist per book
  in a new `Book.graph_layout` JSON column (migration `e0c1d2e3f4a5`,
  plain ADD COLUMN); 'Reset layout' re-runs the auto-layout; 'Export as
  image' downloads a PNG via html-to-image. i18n x8 (ui.relationship_graph,
  17 keys), Vitest (15 in RelationshipGraphView + 2 foundation; pure
  builders + add/remove + create/delete/detail/nav/persist/reset/export
  flows), a Playwright smoke (Aster-run:
  `e2e/smoke/relationship-graph.spec.ts`), DE+EN help page
  (`story-bible/relationship-graph`). Commits: C1 `7bf65655` (foundation),
  C2 `6483a5bf` (nodes/edges/toggle), C3 `6ae3c8f5` (create/delete),
  C4 `debb1220` (detail panel/nav), C6 `370d1d3b` (i18n/smoke/help),
  C5a `fb8e5e27` (Book.graph_layout backend), C5b (frontend persistence +
  PNG + close). Vitest 2605 -> 2620; backend pytest +3 (graph_layout
  round-trip); tsc + verify-theme + docs gates + alembic-upgrade-chain
  regression all green.

## Archived 2026-06-01 — WRITING-HISTORY-STATS-01 (P3)

Third + last of the Scrivener P3 cluster (analysis top-10 #7). Built on
the WritingSession table from WRITING-GOALS-PROGRESS-TRACKING-01, with a
data-model deepening the user opted into: tracking is now per book +
chapter, not just a global daily total.

- **WRITING-HISTORY-STATS-01** (P3) — Backend: `WritingSession` gains
  `book_id` (FK books CASCADE) + `chapter_id` (FK chapters SET NULL);
  the day-unique constraint dropped (one row per book+chapter+day).
  `record_progress` takes book/chapter; `recent_sessions` day-aggregates
  so the #3 daily-goal/streak widget stays correct. New service
  aggregates + endpoints: `/writing-stats/summary` (totals, avg,
  best-day, current+longest streak, daily series), `/by-book`,
  `/by-chapter/{book_id}`, `/export.csv`. Migration `d9b0c1d2e3f4`
  (recreate + legacy-row preserve). Frontend: `WritingHistoryModal`
  opened from the Dashboard `WritingGoalWidget` — window selector
  (30/90/365), 5 summary cards, a recharts per-day bar chart (recharts
  pinned 3.8.1, new dep, 0 vulns), a per-book breakdown drilling into
  per-chapter totals, and a CSV export link. i18n x8 (new
  `ui.writing_stats` block), Vitest, a Playwright smoke (Aster-run:
  `e2e/smoke/writing-history.spec.ts`), DE+EN help page
  (`editor/writing-history`). Commits: C1 `46929496` (backend),
  C2 `12c2a1f7` (frontend), C3 (i18n + docs + smoke + close). Backend
  pytest 2501 -> 2502 (+ alembic-chain regression); Vitest 2601 -> 2605.

  Deferred (no concrete demand): a per-book cumulative-vs-`word_target`
  line chart (needs total-words-ever per book, a different data path
  than the window-based history) and a calendar heatmap. The per-book +
  per-chapter breakdowns the user asked for shipped.

## Archived 2026-06-01 — DOCX-IMPORT-01 (P3, closed by discovery)

Closed as **already shipped**. The Scrivener-analysis filing (top-10
#6) did not notice that **CIO-04** had already implemented end-to-end
DOCX (and EPUB) import: `DocxImportHandler` / `EpubImportHandler`
(Pandoc → Markdown → split-on-H1 → chapters + media extraction,
`backend/app/import_plugins/handlers/office.py`), registered in the
import registry; the generic `POST /api/import/detect` +
`/api/import/execute` orchestrator endpoints; and the
`ImportWizardModal` / `UploadStep` already accept `.docx` + `.epub`.
Verified working: `backend/tests/test_office_handlers.py` — 17 passing
tests (detect/split/execute/overwrite/media/Pandoc-missing/failure).

The only delta closed in this session was documentation: a new DE+EN
help page `import/office` ("Word- und EPUB-Import") was added, since
the Import help section previously covered only git + Medium. The
optional "split on H2 → sections" idea from the original session plan
was NOT built — the H1-only split is the right default (deeper
headings stay inline within the chapter); filed nothing for it as
no concrete demand exists. Commit: `docs(help): Word/EPUB import page`.

- **DOCX-IMPORT-01** (P3) — no code change needed (CIO-04 shipped the
  feature); help-page + archival only.

## Archived 2026-06-01 — CHAPTER-SNAPSHOTS-01 (P3)

First of the Scrivener P3 cluster (analysis top-10 #5). Shipped as an
EXTENSION of the existing `chapter_versions` infrastructure (the
pre-inspection found the model + list/get/restore endpoints + the
`ChapterVersionsModal` already existed from the optimistic-locking
work), not a greenfield `chapter_snapshots` table — the filed scope's
"new table" was re-scoped smaller, as the v0.43.0 handover flagged.

- **CHAPTER-SNAPSHOTS-01** (P3) — Scrivener-style named manual
  snapshots layered onto chapter version history. `ChapterVersion`
  gains `name` (nullable) + `is_manual` (default False); manual
  snapshots are exempt from the last-20 auto-retention trim. New
  endpoints: `POST .../snapshots` (take a named snapshot of current
  saved state), `GET .../versions/{id}/diff` (difflib line-diff of
  snapshot vs current, TipTap-flattened, with a title-change flag),
  `DELETE .../versions/{id}` (manual only; auto rows 400). The
  `ChapterVersionsModal` gains a take-snapshot row, a manual badge +
  name, restore-with-confirm, delete-with-danger-confirm, and a
  back-able diff panel (added/removed tints via color-mix over :root
  `--success`/`--danger`, CSS-first). Migration `c8a9b0c1d2e3`
  (revises `b7f8a9b0c1d2`). i18n x8, Vitest, a Playwright smoke
  (Aster-run: `e2e/smoke/chapter-snapshots.spec.ts`), DE+EN help page
  (`editor/snapshots`). Commits: C1 `03e39546` (backend), C2
  `986b9d82` (modal), C3 `85d2ac32` (diff view), C4 (i18n + docs +
  smoke + close). Backend pytest 2494 passed / 1 skipped; Vitest 2601;
  tsc + verify-theme + verify-docs-discipline + verify-docs-completeness
  green.

## Archived 2026-06-01 — Scrivener Ergonomie-Cluster (P2)

The full P2 cluster from
`docs/audits/scrivener-competitive-analysis-2026-06.md` (analysis
top-10 #1-#4), implemented in order in one session. Each shipped with
backend/frontend, i18n x8, Vitest, a Playwright smoke (Aster-run), and
a DE+EN help page. Final gates: backend pytest 2485 passed / 1 skipped;
Vitest 2593 passed; tsc + verify-theme + verify-docs-discipline +
verify-docs-completeness green.

- **COMPOSITION-DISTRACTION-FREE-MODE-01** (P2) — umbrella
  distraction-free writing mode (Feather toolbar toggle / Ctrl+Shift+D
  / Esc): hides app chrome via a `composition-mode` root class, paper
  backdrop, reuses focus-dimming, adds typewriter scrolling
  (`useTypewriterScroll`). Commits `b5218df7` (core), `c372dab8`
  (typewriter), `dd61b904` (Vitest), `620d3c93` (E2E), `64152a3c`
  (help).
- **CHAPTER-STATUS-LABELS-01** (P2) — per-chapter status (todo/
  first_draft/revised/final) + a user-definable per-book label set
  (`ChapterLabel` table + CRUD + inline manager). `Chapter.status` +
  `label_id` columns (migration `a6e7f8a9b0c1`). Commits `ab25d720`
  (backend), `6d3d5b49` (api), `408f1d98` (UI), `19cd5c9a` (Vitest),
  `75df1f03` (E2E), `54b7b253` (help).
- **WRITING-GOALS-PROGRESS-TRACKING-01** (was P3, promoted P2 +
  scope-augmented with the Scrivener targets/deadline dimension) —
  per-chapter `target_words` (promoted from localStorage to DB),
  per-book `word_target` + `word_target_deadline`, a `WritingSession`
  per-day table (migration `b7f8a9b0c1d2`) recorded by the chapter
  PATCH, and a Dashboard daily-goal + streak widget. STOP-gate
  resolved per user: WritingSession table + DB target. Commits
  `a0e3ae4d` (backend), `d4efb7f1` (api), `ddf152fb` (per-chapter
  target), `fd008777` (widget), `0820e3a5` (book target UI),
  `34450cf1` (E2E + help).
- **CHAPTER-OUTLINER-VIEW-01** (P2) — sortable, inline-editable
  spreadsheet view of chapters (`?view=outline`): position/title/words/
  target/status/label/beat, reusing the status/label/beat selects +
  the optimistic-locked PATCH. Commits `e359e317` (core), `1a57096a`
  (Vitest), `3af55aa7` (E2E + help).

## Archived 2026-06-01

Closed by the v0.43.0 "Story Bible integration depth" release
(10 commits since v0.42.0). The three feature follow-ups filed
2026-05-30 from STORY-BIBLE-STORYBOARD-INTEGRATION-01 all shipped:

- **STORY-BIBLE-RELATIONSHIP-MODEL-01** (was P4) — entity-to-entity
  relationships. Shipped as a `relationships` JSON field on
  `StoryEntity` (Literal type: ally/rival/family/mentor/romantic/
  neutral) + a resolve endpoint + a relationship editor section in
  `StoryEntityEditor` + colour-coded relationship lines in Arc View
  behind a "Show relationships" toggle. Commits `690b55a8`
  (schema), `f22acfda` (editor), `2cace156` (arc-view lines).

- **STORY-BIBLE-PROSE-STORYBOARD-01** (was P4) — the prose
  chapter-card Storyboard. Shipped as four storyboard annotation
  columns on `Chapter` (+ migration) + a new `ProseStoryboard`
  component (separate-component decision per the Stop-Condition,
  not a page-card variant) + the four annotation editors extracted
  to a shared `StoryboardAnnotations` module reused by both
  surfaces. Storyboard button now on every book type. Commits
  `351ca3ac` (schema), `75be8866` (UI + i18n + extraction).

- **STORY-BIBLE-MENTION-EXTENSION-01** (was P4) — @-mention
  autocomplete + auto-detect. Shipped `@tiptap/extension-mention`
  (pinned 2.27.2) in the chapter editor + picture-book page text,
  the `?search=` entity-list filter, the `POST .../auto-detect`
  scan + "link automatically" flow, and i18n across 8 catalogs.
  Comic-bubble text is a plain textarea (not TipTap) -> out of
  scope. Commits `969136da` (mention), `2000d61e` (auto-detect),
  `eeb738c2` (i18n).

Re-scoped (not closed): **STORY-BIBLE-INTEGRATION-DOCS-01** (P4) —
the E2E prose-storyboard smoke shipped (`7fe06345`); the
comprehensive help-doc set + marketing screenshots + full
integrated-flow Playwright spec remain as the deferred remainder.

Newly filed: **COMPONENT-CONSISTENCY-TAIL-01** (P3) — the advisory
remainder of the v0.42.0 component-consistency sweep, deferred from
the v0.43.0 session under budget.

## Archived 2026-06-18 (v0.56.0 release)

- [x] **RELEASE-READINESS-NEXT-CUT-01** (filed 2026-06-17) — closed by the
  v0.56.0 release. The `develop` v0.52.0→v0.55.0 arc was published as a
  catch-up release. The release-gate discipline this item demanded surfaced
  that the E2E smoke gate had been effectively red since ~v0.53.0 (stale
  `.json→.bgb` specs + a flaky tail); the deterministic failures were fixed
  (#421/#422/#428/#423/#434/#435) and the BACKUP-AKZEPTANZTEST cycle + the
  Aster real-browser Playwright run both passed before the tag. Remaining
  E2E flakiness tracked as **#436**.
