plugin:
  name: "kdp"
  display_name:
    de: "Amazon KDP"
    en: "Amazon KDP"
  description:
    de: "KDP-Metadaten, Cover-Validierung und Vorschau"
    en: "KDP metadata, cover validation, and preview"
  version: "1.0.0"
  license: "MIT"
  depends_on: ["export"]
  api_version: "1"

# NOTE: this file is the ZIP-distributable copy. PluginForge reads
# the canonical runtime config from backend/config/plugins/kdp.yaml
# (see "Plugin settings YAML lives in backend/config/plugins/" in
# .claude/rules/lessons-learned.md). No settings block is shipped:
#
# - cover + manuscript requirements: dictated by Amazon, NOT user-
#   editable. Live as KDP_COVER_REQUIREMENTS in bibliogon_kdp/routes.py
#   per the 2026-04-11 plugin-settings audit.
# - categories: also dictated by Amazon. Live as KDP_CATEGORIES in
#   bibliogon_kdp/routes.py per KDP-CATEGORIES-CATALOG-SYNC-01
#   (2026-05-18).
